name: build
on: pull_request

jobs:
  humble_build:
    runs-on:  ubuntu-22.04

    container: ros:humble
    services:
      ros:
        image: ros

    defaults:
      run:
        shell: bash

    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: rosdep update
        run: |
          apt-get update
          rosdep update
      - name: Create Workspace
        run: |
          mkdir -p ~/ros2_ws/src/
          cp -r `pwd` ~/ros2_ws/src/

      - name: Install GeographicLib
        run: |
          apt-get install -y libgeographic-dev geographiclib-tools geographiclib-doc

      - name: Build
        run: |
          cd ~/ros2_ws/
          source /opt/ros/humble/setup.bash
          rosdep install --from-paths src --ignore-src -r -y
          colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release --catkin-skip-building-tests
  foxy_build:
    runs-on:  ubuntu-22.04
    
    container: ros:foxy
    services:
      ros:
        image: ros

    defaults:
      run:
        shell: bash

    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: rosdep update
        run: |
          apt-get update
          rosdep update
      - name: Create Workspace
        run: |
          mkdir -p ~/ros2_ws/src/
          cp -r `pwd` ~/ros2_ws/src/


      - name: Install GeographicLib
        run: |
          apt-get install -y libgeographic-dev geographiclib-tools geographiclib-doc ros-foxy-nmea-msgs

      - name: Build
        run: |
          cd ~/ros2_ws/
          source /opt/ros/foxy/setup.bash
          rosdep install --from-paths src --ignore-src -r -y
          colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release --catkin-skip-building-tests

  jazzy_build:
    runs-on: ubuntu-24.04

    container: ros:jazzy
    services:
      ros:
        image: ros

    defaults:
      run:
        shell: bash

    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: rosdep update
        run: |
          apt-get update
          rosdep update
      - name: Create Workspace
        run: |
          mkdir -p ~/ros2_ws/src/
          cp -r "$(pwd)" ~/ros2_ws/src/

      - name: Install GeographicLib
        run: |
          apt-get install -y libgeographiclib-dev geographiclib-tools geographiclib-doc

      - name: Build
        run: |
          cd ~/ros2_ws/
          source /opt/ros/jazzy/setup.bash
          rosdep install --from-paths src --ignore-src -r -y
          colcon build --cmake-args -DCMAKE_BUILD_TYPE=Release --catkin-skip-building-tests
